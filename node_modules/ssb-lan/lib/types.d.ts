export declare type Discovery = {
    verified: boolean;
    address: string;
};
export declare type SSBConfig = {
    caps: {
        shs: string;
    };
    lan?: {
        legacy?: boolean;
    };
};
