import { AAOLRecord, AddListener, CB } from './types';
declare const Plugin: any;
declare class WordsIndex extends Plugin {
    constructor(log: any, dir: any);
    processRecord(record: AAOLRecord, seq: number, pValue: number): void;
    query(text: string, cb: CB<any>, onAbort: AddListener): void;
}
export = WordsIndex;
